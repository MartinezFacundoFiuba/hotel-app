# hotel-app

## Configuración del Proyecto

Para instalar las dependencias, simplemente ejecuta el siguiente script:

```bash
bash init.sh


## Aclaración
# Si no tienes instalado Python 3, debes instalarlo:

### Verificar si Python 3 está instalado
# **Atención:**
# "Instrucciones para instalar Python 3:"
# - En Ubuntu/Debian: 
#   ```bash
#   sudo apt update && sudo apt install python3 python3-venv
#   ```
# - En Fedora: 
#   ```bash
#   sudo dnf install python3
#   ```
# - En macOS: 
#   ```bash
#   brew install python3
#   ```
# - En Windows: 
#   Descarga el instalador desde [python.org](https://www.python.org/downloads/).
# 
# **Nota:** Kivy funciona con la versión 3.10 de Python. Asegúrate de instalar esta versión.

## Requisitos Previos

# Antes de ejecutar el script `init.sh`, asegúrate de tener instalados los siguientes programas:

# - **Chocolatey** (para Windows): [Instrucciones de instalación de Chocolatey](https://chocolatey.org/docs/installation).
# - **Git**: Para clonar el repositorio y manejar el control de versiones. [Descargar Git](https://git-scm.com/downloads).

## Uso

# 1. Clona el repositorio:
#    ```bash
#    git clone <URL_DEL_REPOSITORIO>
#    cd hotel-app
#    ```

# 2. Ejecuta el script de inicialización:
#    ```bash
#    bash init.sh
#    ```

# 3. Inicia la aplicación:
#    - Para la parte web, ejecuta:
#      ```bash
#      python app.py
#      ```
#    - Para la parte móvil, sigue las instrucciones específicas de Kivy.

## Contribuciones

# Si deseas contribuir al proyecto, por favor sigue estos pasos:

# 1. Haz un fork del repositorio.
# 2. Crea una nueva rama (`git checkout -b feature/nueva-caracteristica`).
# 3. Realiza tus cambios y haz un commit (`git commit -m 'Añadir nueva característica'`).
# 4. Envía un pull request.

## Licencia

# Este proyecto está bajo la Licencia MIT. Consulta el archivo `LICENSE` para más detalles.


